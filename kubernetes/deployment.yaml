apiVersion: apps/v1
kind: Deployment
metadata:
  name: codeflow-engine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: codeflow-engine
  template:
    metadata:
      labels:
        app: codeflow-engine
    spec:
      containers:
      - name: codeflow-engine
        image: autopracr.azurecr.io/codeflow-engine:latest
        ports:
        - containerPort: 8080
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 15
          periodSeconds: 20
        envFrom:
        - configMapRef:
            name: autopr-config
        - secretRef:
            name: autopr-secrets
        resources:
          requests:
            cpu: "250m"
            memory: "512Mi"
          limits:
            cpu: "500m"
            memory: "1Gi"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: autopr-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: autopr-worker
  template:
    metadata:
      labels:
        app: autopr-worker
    spec:
      containers:
      - name: autopr-worker
        image: autopracr.azurecr.io/codeflow-engine:latest
        command: ["autopr-worker"]
        readinessProbe:
          exec:
            command:
            - "true"
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          exec:
            command:
            - "true"
          initialDelaySeconds: 15
          periodSeconds: 20
        envFrom:
        - configMapRef:
            name: autopr-config
        - secretRef:
            name: autopr-secrets
        resources:
          requests:
            cpu: "250m"
            memory: "512Mi"
          limits:
            cpu: "500m"
            memory: "1Gi"
